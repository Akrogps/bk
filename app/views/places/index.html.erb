<!-- Searchbar -->
<div></div>
<!-- Resto cards -->
<div>
  <% @places.each do |place| %>
    <%= place.title %>
  <% end %>
</div>
<!-- Map -->
<div class="col-5"
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
